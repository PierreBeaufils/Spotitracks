<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Spotitrack</title>
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="stylesheet" type="text/css" href="/stylesheets/style.css">

</head>

<body>
    <header>
        <div class="logo-title">
            <a href="/">
                <img class="logo" src="/favicon.png"></img>
                <span class="title">Spotitrack</span>
            </a>
        </div>
        <ul class="navbar">
            <% if (user) { %>
            <li><a href="/tracks/top">Top Tracks</a></li>
            <li><a href="/artists/top">Top Artists</a></li>
            <% } else { %>
            <li><a href="/login">Top Tracks</a></li>
            <li><a href="/login">Top Artists</a></li>
            <% } %>
        </ul>
        <% if (user) { %>
        <a href="/logout" class="logout-button"><button>Logout</button></a>
        <% } else { %>
        <a href="/login" class="login-button"><button>Login</button></a>
        <% } %>
    </header>